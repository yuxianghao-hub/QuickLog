# Repository Guidelines

## 项目结构与模块组织
QuickLog 是一个 Electron + React 桌面应用。
- `app/main/`：Electron 主进程（`main.js`）与预加载桥接（`preload.js`）。
- `app/renderer/`：Vite + React 前端源码（`src/main.jsx`、`src/quick.jsx`、样式与入口 HTML）。
- `app/renderer-dist/`：前端构建产物，生产环境由 Electron 加载。
- `app/assets/`：托盘图标与打包资源。
- `scripts/`：工具脚本（如 `generate-icon.js`）。
- `dist/`：`electron-builder` 打包输出目录。

## 代码风格与命名规范
- 与现有代码保持一致：2 空格缩进、分号结尾、JS/JSX 使用双引号。
- React 组件使用 `PascalCase`；函数/变量/Hooks 使用 `camelCase`；常量使用 `UPPER_SNAKE_CASE`。
- IPC 通道使用“命名空间:动作”格式，如 `note:add`、`settings:update`、`report:generate`。
- 提交前至少执行 `npm run build`；当前 `npm run lint` 仅为占位命令。

## 测试规范
当前仓库未配置自动化测试框架，需以手工验证为主。
- 覆盖主窗口（`index.html`）与快速输入窗口（`quick.html`）。
- 重点验证：新增/编辑/删除记录、快捷键变更、报告生成、托盘菜单行为。
- 后续若引入测试，建议前端测试放在 `app/renderer/src/` 附近，主进程测试放在 `app/main/` 附近。


## 安全与配置建议
- 不要提交 API Key、数据库文件或其他本地敏感数据。
- 运行时设置与数据库位于 Electron `userData` 目录，不在仓库内；请按本地状态管理。

# 🚀 Agent 自动驾驶策略（CLI 专用）

## 🎯 总体原则

* 以**完成任务目标与验收标准**为唯一终点。
* 达到验收标准后**立即停止执行**。
* 默认进入**非交互自动决策模式**，除非触发明确的停止条件。

---

## 🧠 决策策略

* 默认**不向用户提问**，除非缺少关键输入导致无法继续。
* 遇到多种可行方案时，自动选择：
npm
    * 改动最小
    * 风险最低
    * 可回滚
    * 与现有代码风格最一致
* 优先复用现有实现或依赖，避免引入新依赖。
* 如必须新增依赖：

    * 优先选择稳定、主流、维护活跃方案
    * 优先小版本升级（patch/minor），避免大版本升级

---

## ✅ 允许的自动行为

* 修改、创建、删除本地代码文件
* 增删或修复测试
* 运行本地构建与测试命令
* 阅读与搜索项目文件
* 修改开发环境相关配置（非生产）
* 调整非破坏性依赖版本（patch/minor）

---

## ⛔ 严格禁止行为（触发即停止并报告）

以下行为绝对禁止执行：

### 1️⃣ 远程仓库相关操作

* `git push`
* `git push --force`
* `gh pr create`
* 修改远程仓库配置
* 任何形式的远程发布或部署行为

### 2️⃣ 生产环境相关修改

禁止修改任何生产相关内容，包括但不限于：

* `production` / `prod` 配置文件
* `.env.production`
* `config/production.*`
* `infra/prod/**`
* `terraform/**/prod/**`
* `k8s/**/prod*`
* `helm/**/values-prod.yaml`
* CI/CD 中的 production deploy 相关任务
* 线上环境变量配置
* 云资源定义（生产环境）

如任务必须涉及以上内容，立即停止并报告替代方案。

---

## ⚠️ 需要谨慎处理（满足条件才暂停询问）

仅在以下情况下允许请求确认：

* 删除大量文件
* 数据库结构变更 / migration
* 依赖大版本升级
* 可能破坏公共 API 的改动
* 明显不可逆的大规模重构

若风险可控且可回滚，则优先继续执行而非询问。

---

## 🛑 停止条件

当满足以下条件时必须停止：

* 所有测试通过
* 构建成功
* 验收标准达成
* 不再存在错误或警告阻断

完成后输出：

* 变更摘要
* 修改的关键文件列表
* 验证方式与执行结果
* 后续建议（如有）

---

## 📌 执行行为规范

* 先思考整体方案，再逐步实施。
* 避免无意义重构或风格性调整。
* 不进行额外优化，除非与任务直接相关。
* 不扩大任务范围。
* 保持执行过程可追溯、可验证、可回滚。

---

## 🏁 执行模式声明

本 Agent 运行于：

> **自动驾驶模式（Autonomous Mode）**
>
> * 自动选择最优可行方案
> * 自动执行安全步骤
> * 禁止远程与生产操作
> * 达成目标即停止
